---
import Layout from '../layouts/Layout.astro'
import MerchantManagementShell from '@/components/merchants/MerchantManagementShell.vue'
import type { UserRole } from '@/components/dashboard/types'

const role: UserRole = 'admin'
---

<Layout title="商家管理 - Trip 控制台">
  <MerchantManagementShell client:load role={role} activePath="/merchants" />
</Layout>
